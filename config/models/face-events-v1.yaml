schema_version: face-event-config/v1
model_id: face-events-v1
affect_schema_id: affect-vector/v1
affect_dimensions: [valence, arousal, dominance]
controller_response_model_id: maestro-response-v1
calibration_sha256: 8ad9ad1f59dc70c47515c9a37b4531c5070c22a40ed66fb673d0e72a0d3dadb4
controller_settings_sha256: e9695abc1888fef19d5ec4f49d396c7f53c8955335e72e241c6c2851d783d133
decision_interval_s: 0.2
events:
  - kind: blink
    actuator_names: [lower_eyelids, upper_eyelids]
    base_hazard_hz: 0.55
    min_hazard_hz: 0.08
    max_hazard_hz: 0.9
    affect_weights: [-0.1, 0.35, -0.05]
    intensity_weight: 0.2
    refractory_s: 2.5
    recovery_time_constant_s: 0.7
    onset_s: 0.6
    hold_s: 0.2
    release_s: 0.6
    amplitude_min: 0.35
    amplitude_max: 0.5
    polarity: negative
    conflicts_with: [blink, gaze]
    provenance: >-
      Hand-authored research prior with conservative response-feasible phase
      durations; not fitted human or hardware event-rate evidence.
  - kind: gaze
    actuator_names: [right_eye_horizontal, left_eye_horizontal]
    base_hazard_hz: 0.4
    min_hazard_hz: 0.05
    max_hazard_hz: 0.7
    affect_weights: [0.1, 0.3, 0.15]
    intensity_weight: 0.25
    refractory_s: 1.5
    recovery_time_constant_s: 0.6
    onset_s: 0.4
    hold_s: 0.8
    release_s: 0.4
    amplitude_min: 0.08
    amplitude_max: 0.16
    polarity: bidirectional
    conflicts_with: [blink, gaze]
    provenance: >-
      Hand-authored research prior preserving accepted coupled horizontal gaze;
      not fitted human or hardware event-rate evidence.
provenance: >-
  Statistical baseline for explicit sparse facial events. All timings and rates
  are research priors and do not constitute measured performance evidence.
