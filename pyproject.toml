[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "alice"
version = "0.1.0"
description = "Alice robotics perception and learning contracts"
requires-python = ">=3.12,<3.15"
dependencies = [
  "numpy",
  "pydantic>=2,<3",
  "pyserial>=3.5,<4",
  "pyyaml",
]

[project.optional-dependencies]
perception = [
  "mediapipe",
  "opencv-contrib-python",
]
speech = [
  "pocket-tts==3.1.0",
  "sounddevice==0.5.6",
]
conversation = [
  "httpx==0.28.1",
  "onnxruntime==1.30.0",
  "opencc-python-reimplemented==0.1.7",
  "scipy==1.18.1",
]
ml = [
  "safetensors>=0.8.0",
  "torch>=2.14.0",
]
full = [
  "mediapipe",
  "opencv-contrib-python",
  "pocket-tts==3.1.0",
  "sounddevice==0.5.6",
  "safetensors>=0.8.0",
  "torch>=2.14.0",
]

[project.scripts]
alice-speak = "alice.speech.cli:main"
alice-conversation = "alice.conversation.cli:main"
alice-jaw-trial = "alice.experiments.jaw_trial_cli:main"
alice-camera = "alice.perception.cli:main"
alice-passive-capture = "alice.experiments.passive_capture:main"
alice-hardware-preflight = "alice.experiments.hardware_cli:main"
alice-hardware-run = "alice.experiments.hardware_run_cli:main"
alice-motion-readiness = "alice.experiments.motion_readiness_cli:main"

[dependency-groups]
dev = [
  "mediapipe",
  "mypy",
  "opencv-contrib-python",
  "pytest",
  "ruff",
  "safetensors>=0.8.0",
  "torch>=2.14.0",
  "types-pyserial",
  "types-pyyaml>=6.0.12.20260906",
]

[[tool.uv.index]]
name = "pytorch"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

[tool.hatch.build.targets.wheel]
packages = ["src/alice"]

[tool.hatch.build.targets.sdist]
include = [
  "/src",
  "/tests",
  "/pyproject.toml",
]

[tool.uv]
default-groups = ["dev"]
managed = true

[tool.uv.sources]
torch = { index = "pytorch" }

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
target-version = "py313"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.mypy]
python_version = "3.13"
strict = true
packages = ["alice"]
