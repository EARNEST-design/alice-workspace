# HW-649 ESP substitution preparation

The operator identifies an HW-649 Arduino/ESP combo board with physical switches
routing USB to either processor; currently routed to the ESP. They request a
diagnostic firmware flash and will wire it to the translator afterward.

Initial USB inventory finds no new USB serial bridge: only XIAO E0:72:A1:FB:E5:DC
and Maestro 00037376 serial interfaces are present. Chip model, flash capacity,
board variant and switch layout remain unverified. A cable reconnection/data
connection check was requested. No flash write or MCU reset was performed.

Prepared Arduino ESP8266 core 3.1.2 in an isolated private directory:
`/home/alice/.local/state/alice-hardware/hw649-diagnostic-20260916/`.
`usb-preflight.json` retains the initial inventory; `arduino-cli.yaml` and
`core-install.log` identify the toolchain. No diagnostic source/image has been
built yet. ESP identification and private full-flash backup precede flashing.

The intended comparison replaces the XIAO signal source while keeping the
Maestro and level translator. The first electrical comparison can use idle
GPIOs; any subsequent UART query transport must account for the ESP8266 UART0
being shared with the USB bridge. Keep the 5 V to 3.3 V translation for its RX.

Family reference only; not proof of this specific HW-649 variant:
[RobotDyn UNO+WiFi R3 documentation](https://robotdyn.com/uno-wifi-r3-atmega328p-esp8266-32mb-flash-usb-ttl-ch340g-micro-usb).

## HW-649 substitution status, 2026-09-16

The board now enumerates as CH340 at USB 3-2.1, ttyUSB0. An esptool read-only
chip-ID attempt failed with invalid packet header 0x50; this does not identify
the MCU or establish the earlier XIAO fault. ESP download-mode switch/reset
configuration remains unverified. No flash backup or firmware write occurred.
Isolated ESP8266 Arduino 3.1.2 tooling is prepared privately at
/home/alice/.local/state/alice-hardware/hw649-diagnostic-20260916/.
The operator subsequently requested the direct USB serial adapter test above;
HW-649 flashing remains unfinished. Preserve the board until identified and
backed up. Do not use the non-unique CH340 by-id link for flashing.
